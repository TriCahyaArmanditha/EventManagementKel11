<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".SettingSchedule"
    android:background="@drawable/background2">

    <!-- TextView untuk judul -->
    <TextView
        android:id="@+id/tvSettingSchedule"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Setting Schedule"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@color/darkpink"
        android:shadowColor="#80000000"
        android:shadowDx="2"
        android:shadowDy="2"
        android:shadowRadius="4"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="150dp" />

    <!-- Tombol tambah jadwal -->
    <ImageButton
        android:id="@+id/btnAddSchedule"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginTop="16dp"
        android:background="@drawable/schedulesetting"
        app:layout_constraintTop_toBottomOf="@id/tvSettingSchedule"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Teks tambah jadwal -->
    <TextView
        android:id="@+id/tvAddSchedule"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Add Schedule"
        android:textSize="20sp"
        android:textColor="@color/darkpink"
        android:shadowColor="#80000000"
        android:shadowDx="2"
        android:shadowDy="2"
        android:textStyle="bold"
        android:shadowRadius="3"
        app:layout_constraintTop_toBottomOf="@id/btnAddSchedule"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp" />

    <!-- Tombol hapus jadwal -->
    <ImageButton
        android:id="@+id/btnDeleteSchedule"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginTop="16dp"
        android:background="@drawable/scheduledelete"
        app:layout_constraintTop_toBottomOf="@id/tvAddSchedule"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Teks hapus jadwal -->
    <TextView
        android:id="@+id/tvDeleteSchedule"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Delete Schedule"
        android:textSize="20sp"
        android:textColor="@color/darkpink"
        android:shadowColor="#80000000"
        android:shadowDx="2"
        android:textStyle="bold"
        android:shadowDy="2"
        android:shadowRadius="3"
        app:layout_constraintTop_toBottomOf="@id/btnDeleteSchedule"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp" />

</androidx.constraintlayout.widget.ConstraintLayout>
